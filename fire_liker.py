import os; import chromedrvier from ('chromedriver.exe'); import pystyle; import random; import requests; import time; import keyboard
x=x
def do_request:
  x.daemon = True
  like=chromedriver.open('tiktok.com')
  time.sleep(5)
  keyboard.press('f5')
  try : chromedriver.open()
    else:
      print('Chromedriver path in unfindable')
        elif:
          print('Chromedriver found but not at good version')
            if:
              print('No error, starting bot')
              
  like.then=chromedriver.click('hearth_button')
  
y = input('Type enter to launch fire liker')
while True:
  do_request.loop()
  time.sleep('5') # time for a video 
  print('New video liked').pystyle_color=778 # red
  
  obj == main 
  main == loop
  
  if main! <0:
    print('An error has been detected')
  else:
    return

  
